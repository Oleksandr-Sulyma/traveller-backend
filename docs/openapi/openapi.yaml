openapi: 3.0.0
info:
  title: Travel Stories API 🌍
  version: 1.0.0
  description: >
    API для сервісу обміну історіями подорожей.
    Дозволяє користувачам реєструватися, створювати власні історії, завантажувати фото та зберігати цікаві розповіді інших мандрівників.
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html

servers:
  - url: ${API_URL}
    description: "Бекенд (динамічно через змінну оточення)"

# 🔐 Глобальний захист через Access Token у Cookies
security:
  - cookieAuth: []

paths:
  # --- Auth ---
  /auth/register:
    $ref: './paths/auth.yaml#/register'
  /auth/login:
    $ref: './paths/auth.yaml#/login'
  /auth/refresh:
    $ref: './paths/auth.yaml#/refresh'
  /auth/logout:
    $ref: './paths/auth.yaml#/logout'
  /auth/check:
    $ref: './paths/auth.yaml#/check'
  /auth/request-reset-email:
    $ref: './paths/auth.yaml#/request-reset-email'
  /auth/reset-password:
    $ref: './paths/auth.yaml#/reset-password'

  # --- Users ---
  /users:
    $ref: './paths/users.yaml#/root'
  /users/me:
    $ref: './paths/users.yaml#/me'
  /users/{id}:
    $ref: './paths/users.yaml#/byId'
  /users/{id}/private:
    $ref: './paths/users.yaml#/byIdPrivate'

  # --- Stories ---
  /stories:
    $ref: './paths/stories.yaml#/storiesRoot'
  /stories/own:
    $ref: './paths/stories.yaml#/storiesOwn'
  /stories/saved:
    $ref: './paths/stories.yaml#/storiesSaved'
  /stories/{id}:
    $ref: './paths/stories.yaml#/storyById'
  /stories/{id}/save:
    $ref: './paths/stories.yaml#/storySave'

  # --- Categories ---
  /categories:
    $ref: './paths/categories.yaml#/categoriesRoot'

components:
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: accessToken
      description: "Access токен, який встановлюється автоматично в куки після логіну"

  schemas:
    User:
      $ref: './components/schemas.yaml#/User'
    UserResponse:
      $ref: './components/schemas.yaml#/UserResponse'
    Story:
      $ref: './components/schemas.yaml#/Story'
    Category:
      $ref: './components/schemas.yaml#/Category'
    Error:
      $ref: './components/schemas.yaml#/Error'
